# 更新日志

版本号唯一来源是根目录 `VERSION` 文件，构建 / 后端 API / 面板设置页 / 右键菜单统一读取。

| 位 | 何时递增 | 例 |
|----|---------|-----|
| MAJOR | 形态级变化（脚本 → App、架构重写） | 1.x → 2.0.0 |
| MINOR | 新功能 / 新模块（新 Tab、新监控维度、新设置项） | 1.1 → 1.2.0 |
| PATCH | 样式打磨、bug 修复、文案微调 | 1.2.0 → 1.2.1 |

## 历史

- **2.5.0** Codex 额度链路升级为实时架构：优先通过 Codex CLI 自带 `app-server` 读取完整 rate limits，完成事件发生时立即合并对应 rollout 的最新快照，并由后台周期校准兜底；新增带 boot id 与单调 revision 的长轮询推送，面板和菜单栏无需等待固定轮询即可同步更新，慢请求、daemon 重启、账号移除及并发强刷均有顺序保护。Codex notify 集成扩展到所有已发现的 `CODEX_HOME`，支持多行、引号键等 TOML 写法，异常配置失败关闭且卸载时精确恢复原值。额度卡按单/双 Agent 与 1–5 个窗口重新统一布局：单窗口使用整行进度条，多窗口使用最短周期圆环与其余横条；两个窗口时横条中心对齐圆心，三个及以上窗口时横条组整体居中，双 Agent 卡片等高加宽松，状态说明分别归属各自卡片右上角，并完成 Claude/Codex、中英日与长文案适配。Swift 13 项、后端 84 项测试全部通过。
- **2.4.0** 额度展示按上游实际窗口自适应：单窗口改为整行进度条，两个及以上窗口固定为「最短周期额度环 + 其余窗口横条」，双 Agent 卡片统一字号、等高和页脚基线，多账号翻页与四/五窗口小组件边界同步加固；Codex 取消 5 小时窗口后，历史采样改按窗口 id 识别，周额度不再被误记为 5 小时额度。设置中的「展示 Agent」现在会即时联动额度卡、会话、活跃状态、最近完成、用量曲线和桌面小组件，重新启用时自动刷新额度缓存，并修复设置 GET/POST 交错、连续保存失败和快速切换两个 Agent 时的竞态。会话搜索框扩大为 40pt 全宽布局，补齐聚焦态、清除按钮和三语无障碍文案；桌面小组件最小尺寸统一为 360×300，密集额度场景不再裁切。新增 6 项 Swift 设置状态回归和 2 项 Codex 额度采样回归，Swift 6 项、后端 51 项测试全部通过。
- **2.3.0** 会话恢复新增工程目录迁移：原目录不存在时可选择新位置并持久化映射，后续恢复、复制命令与项目用量归属自动使用新目录；支持右键重新选择或忘记映射，取消选择则生成不含失效旧路径的安全恢复命令，多账号 `CLAUDE_CONFIG_DIR` / `CODEX_HOME` 绑定保持不变。用量统计重新校准 Claude/Codex 主会话与子 agent：纳入 Claude 嵌套 subagent 和 Codex 独立子任务，排除 `thread_spawn` 复制的父会话历史，只计启动后的 token 正增量；补齐仅含 `total_tokens` 的历史数据、增量 JSONL 半行、缓存版本迁移和工程路径合并等边界。对于上游从未写入 token 遥测的稳定 Codex 会话不再猜测，面板显示统计不完整提示。新增 23 项相关回归覆盖，后端测试增至 49 项。
- **2.2.0** 会话管理升级为 SQLite 元数据索引：daemon 后台增量扫描 Claude/Codex 全量 transcript，只持久化标题、项目、路径、分支、时间与文件指纹，搜索和列表请求不再逐次读取原始正文；支持完整历史检索、按端过滤与 keyset 分页，当前真实数据搜索由秒级降至约 2–7ms，5 万条模拟数据仍保持约 17–39ms。原生会话页改用 `LazyVStack` 与显式「加载更多」，移除大量行错峰动画，预览缓存限制为 20 条并按 mtime 失效；搜索防抖、请求 generation、索引重试与周期刷新均增加竞态保护，避免旧响应覆盖搜索或已加载分页。会话身份统一为 `tool + account_id + session_id`，置顶、预览、恢复和复制命令均按账号隔离，恢复时显式绑定 `CLAUDE_CONFIG_DIR` / `CODEX_HOME`，修复同 ID 跨账号串号；旧 pin 自动迁移且数据库删除/损坏后可重建，不丢收藏。另补齐重复 transcript 回退、坏 JSONL 行容错、损坏数据库恢复、预览消息重复 ID 等边界，新增会话索引与多账号恢复回归覆盖，后端测试增至 26 项。
- **2.1.3** 完成一轮安全、数据准确性与动效性能加固：自动更新只接受 AgentDeck 官方 GitHub Release，严格校验版本、包体上限、Bundle ID、Developer ID Team ID、签名与 Gatekeeper，安装失败自动回滚；分发时 App 与 DMG 容器均做 Developer ID 签名，公证失败即终止。daemon 所有关键状态改为原子写入，GET/POST API 统一 Host 防护，设置保存串行合并并带重试，避免并发覆盖。用量统计改为 Claude/Codex 增量缓存，Codex 按事件差量归属小时并排除子 agent 重放，显著降低首次后的扫描耗时；活跃会话按进程实际打开的 rollout 与 cwd 判断，过滤子 agent/guardian，修复状态错漏。完成事件增加 daemon 启动标识，通知安装器可升级旧 wrapper，并修正 Computer Use notify 链式配置的递归风险；多账号额度告警按账号独立去重。额度轮播改为稳定叠层切换，极光粒子身份固定，减少布局溢出与无效重绘；补充 14 项后端回归测试、GET DNS rebinding/CSRF 用例，并修复大字体预览裁切。
- **2.1.2** 修 Codex CLI 会话工作状态延迟与误判：长时间 `codex resume` 会继续写入文件名较旧的 rollout，旧逻辑只扫按文件名最新的少量 rollout，导致仍在输出的会话可能被判成「空闲」。现对每个正在运行的 Codex CLI 进程读取其当前打开的 `rollout-*.jsonl` 的真实写入时间，并限定在 Codex sessions 目录下只取 mtime，不读内容；仍在写入的旧 rollout 可即时判为「工作中」。同时 `/api/active` 缓存从 10 秒降到 3 秒，保留 30 秒 busy/idle 写入阈值，降低面板状态刷新延迟并避免短暂停顿时闪烁。
- **2.1.1** 修会话完成弹丸归属重复：Codex 会话结束时不再被旧 Stop hook 误识别成 Claude 完成通知，从而避免同一会话先弹 Claude、再弹 Codex 的双通知。daemon 现在只接受带可信 Claude transcript 的 Claude Stop 事件，并校验 transcript 路径位于 Claude 数据源下、文件名匹配 session id、内容包含同 session/cwd 的用户消息；Codex 继续走 notify 事件源。另补充清理历史 AgentDeck Codex Stop wrapper 的多种旧路径写法，并保留用户自定义 hook。
- **2.1.0** 新版更新横幅升级为本地自动下载安装：点击「下载并安装」后 daemon 直接从受控 GitHub Release DMG 下载到临时目录，面板显示下载/准备/安装进度条；下载包挂载后校验 bundle id 与 Developer ID Team ID，安装前备份旧版 App，失败自动恢复旧版并清理临时 DMG/挂载/staging，成功替换 `/Applications/AgentDeck.app` 后自动重启并移除安装包。并包含 2.0.3 后的完成弹丸去重加固、Codex notify 旧脚本迁移去重、额度告警 warn/crit/reset 配色与品牌图标修正，以及公开文档同步至 SwiftPM + AppKit/SwiftUI 架构。
- **2.0.3** 修面板额度与菜单栏「节奏不一致」+ 手动刷新失灵——根因同一处，皆来自 2.0.2 的「开面板智能强刷」。菜单栏图标(main.swift)与面板读的是 daemon **同一份额度缓存**（key=`claude_quota`/`codex_quota`，TTL=`quota_interval`），理应永远一致。但开面板强刷(force)会去打 Anthropic，引出两个副作用：① **开面板看着「还没更新」**——强刷是数秒级出站，期间面板仍显示上一轮旧值、要等返回才跳；且强刷拿到的实时值常高于菜单栏的缓存值，两者数字对不上（即「没保持一致节奏」）。② **手动 🔄 点了没反应**——开面板的自动强刷吃掉了 daemon 的 **10s 防连点闸**，紧接着用户点 🔄 的那次 force 被去重、只回读缓存 → 看似「刷新无效」。现**开面板只做本地缓存快刷**（<50ms，与菜单栏即时对齐、不再出站闪旧值），**实时最新值由 🔄 显式触发**（force 成为唯一强刷入口，不再被自动强刷抢占 → 必定真重拉并在转圈停下时刷新到位）。
- **2.0.2** 四处修复，重点是一个空闲耗电回归：① **空闲耗电飙升修复（关键）**——面板/小组件用的 `NSHostingView` 关闭时只 `orderOut` 隐藏、并未从窗口摘除，其内 `.repeatForever` 呼吸（额度可取时 live 点）/极光动画会让 SwiftUI 的 display link 继续**每帧离屏重绘**，导致关掉面板后仍持续吃 ~28% CPU（`sample` 实测 `CADisplayLink → ViewGraph.beginNextUpdate` 每帧触发、反复 stroke 全部圆角路径）；现关闭时把承载视图 `removeFromSuperview`（无窗口可渲染 → AppKit 停掉 display link，`@State` 仍存活、重开 reattach 恢复），空闲 CPU 28%→0%。② **额度卡「获取失败」修复**——Codex 上游 `credits.balance` 以**字符串**（如 `"0"`）下发，而模型按 `Double` 解，Codable 全有或全无 → 整个 `/api/quota` 解码抛错 → Claude 与 Codex 两张卡同时显示「额度获取失败」；现金额字段（credits 余额、extra_usage）改「数字或字符串皆容」解析，单字段类型漂移不再拖垮整张额度卡。③ **会话完成弹丸去噪**——临时目录（`$TMPDIR`=`/var/folders/.../T`、`/tmp` 等）里的 headless/子 agent 一次性会话也会触发 Stop 钩子，其完成弹丸标题空、项目名被解析成「T」纯属噪音；daemon 现按 cwd 落在临时目录直接丢弃。④ **开面板额度更新更及时**——把额度查询间隔调大（可至 30 分钟）后开面板会一直看到旧值；现开面板时若数据够旧（>3 分钟）才强刷一次（节流到 180s + daemon 10s 防连点闸，避免频繁出站撞 429），强刷失败回退到 last_good 陈旧数据而非刷成「获取失败」。
- **2.0.1** 灵动岛弹丸两处修复：① **图标与 agent 对齐**——额度告警事件的 `tool` 由 daemon 大写传入（`Claude`/`Codex`），而壳层 `appIcon` 按小写判断，导致 **Claude 告警弹丸错用 Codex 图标**；现壳层 `lowercased()` 大小写不敏感比对、daemon `_push_alert` 也存小写 `tool`（与会话完成事件一致，消息文案仍用显示名），所有弹丸图标都跟 agent 一致。② **会话完成不重复提醒**——`pollEvents` 原先把 `since=` 返回的事件全部推送、只从 `last` 推进游标，并发/重叠轮询（典型为休眠唤醒后多请求带同一 `since`）会重复返回同一事件 → 同一完成弹两次；现改为**按事件 `id` 严格大于游标才弹**（main 队列串行无竞争），首轮只对齐游标不弹，杜绝重复。
- **2.0.0** 形态级重写：面板 UI 从 WKWebView 单文件 HTML **整体重写为原生 SwiftUI**（工程迁到 Swift Package，仍零第三方依赖）。daemon(`agentdeckd.py`) 与全部 HTTP API 不变，行为逐项对照 v1 原生复现。要点：① **原生壳**——菜单栏图标 / 玻璃面板 / 桌面小组件 / 灵动岛完成提醒 / daemon 守护全部 AppKit 原生，退役 WebView + scheme-handler + JS 桥。② **完成事件开箱即用**——daemon 启动**幂等自动配置** Claude Stop 钩子 + Codex notify（单槽被占时链式转发回原工具如 Computer Use，均不覆盖用户已有配置）；卸载（`build.sh uninstall` / 直接拖废纸篓）经自清理 wrapper **精确还原**改动。③ **额度告警统一灵动岛弹丸**——原 `osascript`「脚本编辑器」通知改走事件流，warn/crit/reset 分级配色。④ **面板高度自适应内容**（消除底部空隙）、宽度可拖拽持久化。⑤ **小组件更贴近原生**——卡片扁平化、边缘缩放光标。⑥ **大幅降耗电**——断开菜单栏图标 KVO 自激死循环（空闲 CPU 80–126%→0%），呼吸动效改渲染服务器驱动（≈v1 CSS）、定时器降频+合并唤醒。⑦ 修复一批 v1↔v2 行为偏差（设置读不回、API 等值/项目 Top/成本拆分因数字键解码丢失、刷新自旋、字重偏粗、分发资源包崩溃等），经两轮 codex CR 验证。
- **1.25.17** 概览额度卡 & 会话页一轮打磨 + 邮件反馈/Codex 刷新两处兜底：① **额度卡 Claude/Codex 左右两边视觉对齐**——`.wlabel b`（百分比）固定 `min-width: 30px` + 右对齐，消除「35%」/「0%」/「13%」宽度差挤压左侧标签栏；`.qmeta` 改为 `justify-content: center`，单副窗口（Codex 周限额）与双副窗口（Claude 周限额 + 周限额·Sonnet）都以环图为中线居中；副窗口 cd 切紧凑版（新增 `cd.inH` 三语条目）→「9 小时后重置」单行可完整显示，不再被 ellipsis 截。② **会话页过滤按钮 / 搜索框** 调大到与概览同级——`.chip` 9px / padding 3·8 → 10.5px / padding 4·10，`#search` 9.5px / 86px → 10.5px / 100px（focus 140px），`.stitle` 12 → 12.5px 贴齐 `.qname`。③ **正在运行 · 路径字号** `9px / 500` → `10.5px / 600`，与右侧「已运行 X」基本同字号，monospace 字形不变。④ **概览主窗口标签栏精简**——丢掉冗余的「· 已用」（环图百分比已表达），副窗口在标签下追加倒计时（`<em>` 暗色弱化）。⑤ **邮件反馈兜底**——没默认邮件客户端的机器 `NSWorkspace.open(mailto)` 静默失败，用户点了没反应；现在 Swift 桥抽出 `openMailto`，先 `urlForApplication(toOpen:)` 探处理器，探测失败或 open 返回 false → 邮箱复制到剪贴板 + 三语 NSAlert（按 `Locale.preferredLanguages` 切 zh/ja/en），同时引导走 GitHub Issues。⑥ **Codex 手动刷新对外说明**——Codex 没官方 usage API，daemon 是从 `~/.codex/sessions/.../rollout-*.jsonl` 反解最近一行 `rate_limits`；刷新会清缓存重读盘，但 Codex 进程没新请求时 rollout 文件就没新数据 → 解析回的还是旧值（卡底「采样于 X 分钟前」即此意，非 daemon 偷懒）
- **1.25.16** 反馈入口 + 趋势图缩面板被切修复：① 设置最末新增「反馈」段，**GitHub Issue / 邮件**两键并列——预填 `AgentDeck 反馈 v<版本号>` 标题、留正文 + 引导，不携任何账号 / 路径 / 诊断数据（保留个人发问场景的最小披露原则）；Swift 桥 `open:` 通道在原 `http/https` 基础上放行 `mailto:`，桥的其他白名单不变。② 修面板拖矮时趋势图底部图例被切——根因是 `#usagesvg / #curvesvg` 钉死 `min-height: 144px`、且 JS 渲染端 `viewBox` 高度也走 `Math.max(144, ...)` 下限，于是面板矮于 144 时整图被默认 `preserveAspectRatio="meet"` 等比压缩成原高 50%，文字一并被缩成 ~3.5px 不可读、底部图例溢出可视区被切；现两侧均改为「跟随容器实际高度」（CSS 下限 44、JS viewBox 下限 60），文字保持屏幕 7px 不变形，整图按面板高度有机收缩。③ 概览页接入 `#slist` / `#setbody` 同款 `--fadeBtm` 滚动渐隐——当趋势卡仍超出可视区时底部出现淡出，提示用户可滚动查看后续内容（滚动条仍隐藏，维持原有视觉风格）
- **1.25.15** 会话列表两处优化：① **过滤内部摘要/压缩会话**——Claude Code 每次 `/compact`、自动记忆摘要会在 `/private/tmp` 跑一次性 housekeeping 会话（首条消息恒为「You are summarizing a Claude Code session」「Apply maximum non-destructive compression」），它们 mtime 很新，原先白白占掉「最近会话」前排名额，把真实项目会话挤出列表（实测最近 20 条里占 8 条）；现按前缀识别并跳过，真实会话回到列表。② **搜索改为查全量磁盘历史**——此前搜索只在已加载的「最近 N 条」里过滤，关掉终端、或近期忙于别的项目后，老会话虽文件仍在却搜不到；现搜索走服务端全盘扫描（`/api/sessions?q=`）：Claude 利用项目目录名已编码完整 cwd 做路径短路命中（零额外读盘）、路径未命中者在读取预算内读首部做标题匹配，Codex 扩大扫描窗口读首部匹配，结果上限 60；前端 250ms 防抖、带乱序保护，清空即回默认列表。③ **磁盘索引短缓存抗大历史**——搜索的主要开销是「glob + 对全部文件 stat 取 mtime 排序」(O(N))，连续按键会反复付；现把 (mtime,path) 索引缓存 5s 跨按键复用，输入时只剩「有上限的读取」开销：实测会话攒到 1.2 万时热缓存检索 107ms→23ms、3 万时 249ms→50ms、6 万仍 ~80ms，长期积攒也不卡（冷启首次仍建一次索引，万级以下无感）
- **1.25.14** 优化桌面小组件拖动交互：① 顶部细把手够不到/被遮挡时无法拖动——现可**按住 ⌘ 在小组件任意位置拖走**（不按 ⌘ 时点击开面板、多账号 carousel 左右滑动照常，零影响）；② 小组件出现位置**夹回可视范围**，保证顶部拖拽条始终在屏内、不被菜单栏或屏幕边缘盖住（修历史保存位置来自已断开外接屏 / 贴边到把手够不着处）；③ 顶部拖拽条加高 22→28px 更易抓取
- **1.25.13** 手动刷新（🔄）改为**真正重拉**额度，而非只回读缓存：此前点刷新只是重新读服务端缓存，把「额度查询间隔」调大（如 1 小时）后，缓存没到期就一直显示旧数据、点刷新也不变（实测面板 37% 而真实已 55%）。现手动刷新会清掉额度缓存键逼一次真实重拉，带 10s 防连点闸避免狂点撞 429；后台自动轮询与桌面小组件仍按设定间隔走缓存，不增加接口压力
- **1.25.12** 修 1.25.10 引入的副作用——IDE 注入的会话目录被误当成独立计费账号：Xcode 的 `ClaudeAgentConfig` 被当账号去拉额度，既多打一次 `oauth/usage` 撞 **429**、又在额度卡 carousel 里多出一张「ClaudeAgentConfig·429」废卡。现把**仅经在跑进程环境发现**的目录标记为 `session_only`——只读会话 / 活跃 / 用量，不计入额度账号、不拉 usage；被其它途径（用户自设 `CLAUDE_CONFIG_DIR`、`~/.claude-*` 等）发现的目录仍按账号先到先得，单账号时额度卡恢复无 carousel
- **1.25.11** 修运行列表里同一项目重复出现：Claude Code 后台跑的一次性 `claude -p` 助手（标题生成、话题判定等）和子代理也被进程扫描当成会话列了出来——它们一闪而过、还跟随主会话目录，于是同一路径冒出第二行「0 分钟·空闲」。这类助手同样会写 `kind=interactive` 的 pidfile，光看 pidfile 区分不了；现改按**进程父链**过滤：祖先里有另一个 claude/codex 的，即 agent 自己拉起的子进程，不计入运行列表，仅保留用户实际发起的顶层会话（终端 / IDE 宿主拉起）
- **1.25.10** 修 IDE 内置 agent（如 Xcode 自带 Claude）会话状态恒显「空闲」+ 运行列表状态 chip 渲染错位：① **数据源发现新增「在跑进程环境」一路**——IDE/扩展常把 `CLAUDE_CONFIG_DIR` / `CODEX_HOME` 只注入所拉起子进程的 env（既不写 shell 配置、也不在守护进程自身环境里），守护进程原先发现不到；现从在跑的 claude/codex 进程环境直接反查并纳入，Xcode 自带 claude 的 pidfile / transcript / 会话 / 用量随之全部可见（之前它虽被进程扫描命中、却因取不到 pidfile 而无状态来源）。② **claude 忙闲改用 transcript 最近写入活跃度兜底**（与 Codex 同口径，30s 内有写即「工作中」）——Xcode 自带的是旧版 claude，其 pidfile 不带 `status` 字段，此前只能退回按 CPU 占用判忙闲，而流式生成是网络 I/O 密集、CPU 又是全生命周期均值，于是恒显「空闲」。③ **pidfile status 词表归一**——实测 status 不止 busy/idle 还有 `shell`(跑命令) 等工作子态，旧逻辑「非 busy 即空闲」把正在跑命令的会话误显为「空闲」、且原始值落到 chip class 上无对应样式致 chip 失样；现除明确 `idle` 外的工作子态统一归「工作中」，运行列表 chip 始终正确渲染
- **1.25.9** 收尾两处 CR 遗留：① `quota_interval` 变更**即时生效**——把现有「成功」额度缓存的到期改为 `now+新间隔`（调大即刻延长静默、不强制重拉），退避/降级条目(stale/429)不动以免在限流中提前重试；② `get_settings()` 加 **mtime 缓存**——按 `settings.json` 的 mtime 命中，免去每个请求都读盘+解析（保存时主动失效），高频轮询下减少冗余 I/O
- **1.25.8** 调色盘配色协调性补全 + Codex CR 修复：① 自定义主色后**徽章圆形底色/描边、卡片辉光、用量周柱与今日环的 Codex 段、菜单栏品牌 chip** 之前仍是写死的橙/青——现统一用 `@supports (color-mix)` 跟随主色（旧 WebKit 无 color-mix 则回退 rgba 定值，不破样式），不再出现「glyph 变蓝、圆圈还橙」的不协调；模型家族色（opus/sonnet/haiku）按语义保留。② CR 修复：修 `api_quota` 在未发现 Claude 配置目录时引用未定义变量导致 500（NameError）；`_keychain_item` 补捕 `subprocess.TimeoutExpired`，`security` 卡住时跳过该候选继续探测而非中断
- **1.25.7** 设置文案与排版打磨：① 额度查询间隔改为 **Claude/Codex 通用**（去掉「Claude 专属」，Codex 为本地读取一并节流），键名 `claude_quota_interval`→`quota_interval`；② 菜单栏维度文案专业化——「显示哪个窗口」→「百分比窗口」、「颜色看哪个窗口」→「告警依据窗口」，选项「最吃紧」→「用量最高」、「短窗口(5h)」→「5 小时」；③ 通知阈值改为「一级提醒阈值 / 二级严重阈值」并补说明，消除「严重阈值是什么」的困惑；④ 修长标签被 chips 挤成逐字竖排——`.setlabel` 加 `min-width`，并精简各项 hint 文案
- **1.25.6** 修「调了采样间隔仍被限流」+ 额度查询间隔可调到 6 小时：根因是真实 Claude 额度 API 调用频率一直由**写死的 5 分钟 TTL** 控制，而设置里的「额度采样间隔」只影响历史曲线记录密度、**根本不decide API 调用频率**——所以把采样间隔调到 60 分钟也照样每 5 分钟打一次接口、多账号 ×N 更易限流。现新增独立设置「Claude 额度查询间隔」直接驱动该 TTL，**默认 10 分钟（原 5 分钟）、可调至 6 小时**；额度按 5h/7d 计本就无需勤查。两个间隔语义已在设置 hint 里区分清楚
- **1.25.5** 菜单栏额度维度可选 + 修「红配 0%」：此前菜单栏**数字固定取 5 小时窗口、颜色却取所有窗口最大值**，导致 5h 刚重置(0%)但周限额快满时出现「红色 + 0%」的矛盾显示。现新增两项设置——「显示哪个窗口」(数字) 与「颜色看哪个窗口」(变色)，各自可选 **短窗口(5h) / 周限额 / 最吃紧(任一最高)**；用抽象维度映射到各 agent 自己的窗口，**同一设置同时适配 Claude(5h+周+Sonnet/Opus 周) 与 Codex(主/次窗口)**。**默认两者都用「短窗口」**——5h=0% 即显示 0% 常规色，矛盾消失；想要「周限额快满即告警」的用户把「颜色看哪个窗口」设为「最吃紧」即可（即旧版的默认行为）
- **1.25.4** 主题配色（调色盘）：设置「面板」区新增「主题配色」——用取色器自定义 Claude / Codex 主色，额度环、进度条、cost 分段、用量曲线、Logo 渐变全部同步换色（`-deep` 深色变体按 0.82 系数自动加深）；配「恢复默认」一键清空回内置橙/青。实现上把额度环/用量图的 SVG 渐变 stop 与多处品牌色硬编码改为引用 `--claude/--codex` CSS 变量；后端 `color_claude/color_codex` 仅接受 `#rrggbb` 或空串（正则校验，防 CSS 注入）。注：SVG 的 `var()` presentation 属性运行时不响应式重绘，故颜色变更时主动重渲额度环 / 用量图
- **1.25.3** 「展示 Agent」开关 + 多账号 token 按目录精确取 + Codex CR 加固：① 设置新增「展示 Agent」（show_claude / show_codex），只用其中一个的用户可隐藏不需要的板块——隐藏后额度卡消失、双列网格收成单列、会话/活跃/最近完成列表一并过滤，**且后端不再为隐藏的 agent 拉额度**（省钥匙串读取 / Anthropic usage 调用 / 限流消耗）；② 钥匙串改为按 config dir 取对应项——macOS 新版 Claude Code 多账号按 `sha256(目录路径)` 后缀「分项」存凭据，逐候选探测命中即点亮；**共享（无后缀）项仅归默认目录**，非默认源未命中自己的分项即老实显示「无额度」，不再误取主账号 token 造成额度错配；③ CR 加固：`/api/diag` 加 Host 校验（封 DNS rebinding 读取凭据指纹/路径）、自定义 scheme handler 强制 `host==app`、菜单栏账号名截断防撑宽
- **1.25.2** 多账号额度修复 + 旧系统/Intel 适配 + 自动诊断：① **额度取不到（多账号）**——根因是 macOS 上 Claude Code 的订阅 token 存钥匙串、按 `$USER` 存「一条」（不按 config dir 区分），而旧逻辑「非默认源只查目录文件、默认源才查钥匙串」正好与「订阅在非默认的 .claude-personal、默认 .claude 是网关」错位。改为：目录自带 `.credentials.json` 优先；否则按 `settings(.local).json` 的 `ANTHROPIC_BASE_URL`/`ANTHROPIC_AUTH_TOKEN` 识别网关源并排除（不误取订阅 token、标记「无官方额度」），其余源一律回落共享钥匙串 → 订阅账号自动点亮；② **修 macOS 14.x / Intel 打不开**——`build.sh` 此前未指定部署目标，在 macOS 26 上构建会把二进制打成 `minos 26 + arm64-only`，于 14.4.1 等旧系统直接拒绝启动、Intel 机也跑不了。现固定 `-target *-apple-macos13.0` 并产出 **arm64+x86_64 通用二进制**（`NSCursor.FrameResizePosition` 等 15+ API 收进 `if #available` 守卫）；③ 新增 `GET /api/diag` 账号/钥匙串自动诊断（脱敏，不含 token 明文），面板打开即可定位「某账号为何没额度」
- **1.25.1** 修在企业代理环境下面板空白 / 拉不到数据：根因是某些企业安全软件装的系统级 PAC 把 `127.0.0.1` 也改道到 SOCKS 代理，代理到不了回环 → WebView 与 Swift 原生请求都连不上本机 daemon（`curl` 直连正常、daemon 也正常，唯独 App→daemon 被改道）。修法是让 App 连自身 daemon 的流量绕过系统代理：① Swift 原生请求（菜单栏额度 / 事件轮询 / 健康探测）统一走显式禁用代理（`connectionProxyDictionary` 关 HTTP/HTTPS/SOCKS/PAC）的 `URLSession`；② WebView 改用自定义 scheme `agentdeck://`，页面与所有 `/api` 请求经 `WKURLSchemeHandler` 用禁代理 session 转发到 `127.0.0.1`，WebView 自身不再直发 HTTP 到回环，从根上绕开 PAC——POST body 经注入的 fetch 包装以 `X-AD-Body`(base64) 头透传（规避 `WKURLSchemeTask` 拿不到 httpBody 的限制），handler 转发时剥离 `Origin`/`Host` 以过 daemon CSRF
- **1.25.0** 多账号并行支持：① 后端把写死的 `~/.claude`/`~/.codex` 单路径改为「数据源发现」——综合 `CLAUDE_CONFIG_DIR` 环境变量、`~/.claude-*` 通配（命中 `-personal`/`-work` 隔离目录）、反查 shell 启动文件里的 `CLAUDE_CONFIG_DIR=`（守护进程看不到别名注入的 env）、设置内手动添加四条线索发现目录，并校验「确为 Claude 配置目录」排除 `.claude-mem` 等同名异类目录；② 多账号额度：每个目录优先读 `.credentials.json` 各拉一次官方 usage，默认目录无文件回落钥匙串，网关账号（idealab 等 `ANTHROPIC_AUTH_TOKEN` 形态）官方无额度端点故标记「仅用量」；③ 会话/活跃/用量/预览按多源遍历并打 `account` 标签；④ 面板 Claude/Codex 板块多账号时变 carousel（左右滑动 + 圆点翻页 + 账号标签，单账号零变化）；⑤ 菜单栏新增「多账号轮转」（按间隔逐账号轮显图标+额度，`0`=关闭维持主账号常显）。`/api/quota` 向后兼容：`claude`/`codex` 仍为主账号单对象，新增 `accounts.{claude,codex}[]` 列表。限流加固：默认账号沿用稳定缓存键 `claude_quota`（多账号化不丢历史降级兜底）、降级值只存成功结果（`ok=False` 不再污染兜底，429 时能回退上次好数据）、Claude 额度缓存 120s→300s（官方 usage 端点限流偏严，额度窗口 5h/7d 无需勤拉）、`cached` 加 per-key 计算锁杜绝缓存击穿（Swift/前端/采样器并发到期不再各打一次外部接口）
- **1.24.10** 修更新后旧后台服务不自动清理 / 新版仍跑旧后端：① App 启动时 `ensureBackend` 改为**版本感知**——端口上的 daemon 版本须等于 App 自身版本才复用，不符则杀掉重起（daemon 是常驻进程，换了磁盘上的 `.py` 不会自动重载，必须强制换新才跑新代码；此前只看 `ok` 就复用，导致装了新版后端行为仍是旧的）；② daemon 新增父进程看门狗线程，壳 App 退出后（被 launchd 收养、PPID→1）自杀，杜绝复用场景下 daemon 脱离 App 残留成孤儿堆积。自 1.24.10 起更新即干净；同 bundle ID 下「旧 App 没退就启新版只激活旧实例」是 macOS 系统行为，仍需先退旧
- **1.24.9** 修 `auto`（跟随系统）语言偶发被锁成英文且切不回来：系统语言探测（`defaults read -g AppleLanguages`）走 5 分钟缓存，而探测失败此前一律回退 `en` 并连同失败结果一起缓存——App 启动瞬间 daemon 与壳同时拉起、subprocess 争用时若探测抖一下，英文就被锁 5 分钟，面板正好在此窗口渲染即显英文。现改为探测失败不写缓存（本次临时回退 en，下个刷新周期立即重探恢复），中文/日文系统不再被启动竞态锁成英文
- **1.24.8** 修菜单栏图标在 macOS 26 显示为黑色：根因是系统对模板图设置 `contentTintColor` 会整体渲染成黑（对照实验定位），告警色改为直接烘进图标位图；告警色按 agent **分段独立**（37% 与 92% 不再一起变橙，常规段按菜单栏明暗自取黑白，外观变化即时重绘）；新增「额度告警变色」设置开关（默认开）
- **1.24.7** 活跃会话状态判定补 CPU 兜底：Xcode 内置 Coding Assistant 等 headless 拉起的 claude（不写 transcript、无 pidfile）此前状态为空、不显示忙闲呼吸标记，现按进程瞬时 CPU 占用（≥5% 为工作中）推断
- **1.24.6** 修 Codex 额度环误显 Claude 橙色：`.ring .arc` 的 CSS `stroke` 写死了 `grad-claude`，而 CSS 优先级高于 SVG 表现属性，把各卡片按品牌指定的渐变全部覆盖（潜伏已久，小组件上对比明显才暴露）
- **1.24.5** 字体缩放的窗口过渡改为**交叉淡变**（淡出 → 隐形时一步切尺寸并等内容绘制提交 → 淡入）：WKWebView 渲染在独立进程异步提交，任何「边框动画 + 内容追赶」的同步方案都可能出现玻璃先到、内容后到的脱节，淡变从机制上规避竞态
- **1.24.4** 字体缩放全面适配：面板/小组件**窗口尺寸随缩放系数同步放大**（布局有效宽度恒定，根治放大后挤压截断；尺寸记忆按未缩放基准存取防复利），窗口过渡用 GPU 合成动画——一步到位仅一次重排、内容层 CASpringAnimation 弹性缩回（frame 逐帧动画会让 WKWebView 每帧重排必卡）；额度卡片文字收窄时换行不再裁切；面板四周边距统一 14px
- **1.24.3** 开关动效深度优化（采纳 Codex 渲染管线评审）：动画严格收敛至合成器属性——去掉 width 过渡与按压拉伸（布局属性逐帧 relayout、中途改位移目标产生二段感）、轨道层显式提升、`pointerdown` 即响应（click 要等抬起）、保存副作用按 key 精准应用且延至动效播完（实测滑动窗口内零丢帧）。修字体放大后概览被遮挡无法滚动：`vh/vw` 不随 zoom 缩放，统一除以 `--fzoom` 补偿。README 头部加应用图标
- **1.24.2** 修开关切换卡顿：点击改为乐观更新（立即翻转、动画完整播放，保存失败回滚），开关保存不再整页重建设置 DOM（此前重建会把进行中的动画打断成跳变）
- **1.24.1** 设置开关动效对齐 iOS 质感：轨道渐变层 opacity 交叉淡入（替代背景硬切）、滑块弹簧曲线位移、按住横向拉伸松手回弹（off 锚左缘 / on 锚右缘）
- **1.24.0** 字体大小设置（80–160% 整体缩放，面板与小组件同步生效）；设置内新增「手动检查更新」（绕过 6 小时缓存强制查询，无新版 / 失败均有明确提示）；设置项按常用度重排——「面板」（语言 / 字体 / 外观）提到最前，告警阈值类后移；面板支持 `?tab=` / `?settings=1` 调试参数；README 效果图全面重制（紧凑高度、视网膜 2x、会话页演示帧、两图等高对齐），致谢与功能清单同步至最新
- **1.23.1** 更新清单与官网迁移至自有域名 `agentdeck.yilin.dev`（部署时发现 `agentdeck.pages.dev` 已被他人项目占用——更新清单绝不能放在不受控域名下，落地页 canonical / OG 与 README 同步更正）
- **1.23.0** 新版本发现：daemon 定时拉取静态清单（`agentdeck.pages.dev/version.json`，6 小时缓存，仅比对版本号、不带凭据与本机信息），面板顶部出现可关闭的更新横幅，点击经默认浏览器打开下载页；设置内新增「检查更新」开关（默认开）。新增 `site/` 官网落地页（暗色液态玻璃 + 极光背景，中 / EN / 日三语自动检测，零依赖单文件，可直接部署 Cloudflare Pages），更新清单与落地页同源托管
- **1.22.2** 跳转会话成功后自动收起主面板，让目标终端独占前台（此前误发 `panel` 消息反而把 AgentDeck 重新激活，与刚聚焦的终端抢焦点）
- **1.22.1** 修跳转会话卡死：宿主识别会把 macOS `Python.framework` 内置的 `Python.app` 误当宿主，随后 `tell application "Python" to activate` 挂起 12s。改为**跳过 `.framework` 内置 bundle 与解释器/自身**，且通用激活改用 `open <bundle 路径>`（命中已运行实例、绝不挂起）；识别不到宿主时优雅回退而非超时
- **1.22.0** 多语言界面（简体中文 / English / 日本語，默认跟随系统、设置内即时切换，面板 / 后端通知 / 应用壳菜单三层文案统一，有效 locale 由 daemon 单一解析下发）；活跃会话期间保持系统唤醒，防长任务因休眠 / 断网中断（默认开，`caffeinate` 绑定 daemon pid 自动随退出释放）；跳转会话自动识别宿主终端——沿进程链定位最外层 `.app`，兼容任意终端、不再依赖固定清单，且宿主已知时只激活宿主、不再盲探 iTerm / Terminal（修「跳 Warp 等终端却误启动 Terminal」）；会话恢复终端矩阵扩充——新增「打开 App + 复制命令（粘贴回车恢复）」一档，覆盖 Warp / VS Code / Cursor / Windsurf / Hyper / Tabby / Rio / Wave 等无法注入命令的常见终端；桌面小组件跟随主面板设置（语言 / 外观）即时收敛，补齐多语言适配
- **1.21.5** 开源发布准备：bundle id 中性化（com.agentdeck.app）、补 LICENSE/CHANGELOG、README 公开化重构
- **1.21.4** 滚动渐隐改为按位置动态驱动（贴顶/贴底自动收起，不再遮挡首行）
- **1.21.3** 滚动边缘渐隐替代硬切边
- **1.21.2** 开关泛白真因修复——WebKit 透明边框下背景双重绘制的 AA 接缝（background-clip: padding-box）
- **1.21.1** 边缘缩放光标提示（borderless 窗口无系统反馈，悬停边带显示对应方向箭头）；修开关开启态端帽泛白月牙
- **1.21.0** 对标系统小组件的视觉三连：连续曲率圆角（squircle）、顶亮底暗渐变描边（RimView）、三级文字对比度提升
- **1.20.2** ⓘ 按钮去双圈（ghost 风格）+ i 点加大
- **1.20.1** 口径说明改 ⓘ 弹层：今日条与趋势卡各挂入口，玻璃弹窗分视图展示对应口径，含动态模型明细与双端分项
- **1.20.0** 通知只发一次（三通道闭环）：回灌事件标记 replayed 不再弹岛、壳首轮只对齐游标、额度告警状态持久化跨重启；设置页 hint 全面精简
- **1.19.6** 菜单栏兜底字形换装：双环单色线稿（与 App 图标同构）
- **1.19.5** App 图标重设计：双 agent 活动环 + 品牌双色弥散底
- **1.19.4** 小组件再打磨：窄卡防拆行、隐藏 0% 次窗口行、默认尺寸按内容实高
- **1.19.3** transcript_path 限定 `~/.claude` 内（全服务唯一「输入→读文件」路径收口）
- **1.19.2** CSRF P1 加固：Origin/Content-Type 结构化解析（防前缀/子类型绕过）、Host 校验封 DNS rebinding；回归脚本 `scripts/test-csrf.sh`
- **1.19.1** 外部评审复审采纳：POST 接口 CSRF 屏障、stale 警示优先级提前、burnHint NaN 兜底、重置条按窗口 id 解耦文案、健康检查身份校验
- **1.19.0** 外部评审采纳：额度文案语义化、采样过旧醒目警示、精简模式开关、数据管理区（打开目录/导出 CSV/清空记录）、事件标题图片标记清洗
- **1.18.1** 桌面小组件排版修复，小组件纳入今日摘要条
- **1.18.0** 首页信息升级：今日摘要条（迷你模型环 + 今日 token/等值$/24h 环比）、最近完成事件流（点击跳转会话）、项目 Top 榜（近 7 天）、额度窗口重置进度微条
- **1.17.3** 用量图弹性生长填满面板（144–300px 自适应）；修模型键版本化后柱状图堆叠回归
- **1.17.2** 默认面板高回调至 780pt
- **1.17.1** 设置分区标题放大提亮；默认面板高按最长页面取值，弹出时钳制屏幕可视高度
- **1.17.0** 恢复会话终端矩阵扩充（Ghostty/kitty/WezTerm/Alacritty 命令行直启 + iTerm2/Terminal AppleScript）；设置改下拉框，仅列出本机已安装终端
- **1.16.6** 修状态栏入口消失：禁止 ⌘拖拽移除 + isVisible 周期性自恢复 + 合成图宽度合法性兜底
- **1.16.5** 终端跳转支持扩充：cmux、Tabby、Hyper、Rio
- **1.16.4** 统计口径多轮修复（对照 ccusage 校准至 ±4%）：Claude usage 行按 (message.id, requestId) 去重；Codex 改用 total_tokens（cached/reasoning 是子集）；Opus 4.5+ 降价单价表；cache 写入按 5m/1h 分档计价；新增 Codex API 等值
- **1.16.3** 修额外用量金额单位：接口返回美分，按美元换算展示
- **1.16.2** 文案本地化：Extra Usage → 额外用量
- **1.16.1** 「常显用量」收成一行多选 chips
- **1.16.0** 菜单栏 agent 可多选：Claude/Codex 独立开关，全不选仅留图标
- **1.15.4** 修 Codex 完成事件未提取 thread-id/cwd，弹窗点击只能兜底开面板
- **1.15.3** 识别 Codex 桌面端活跃会话（rollout 近期写入 + originator 判定）
- **1.15.2** Codex 桌面端会话跳转走 `codex://threads/<id>` 深链直达线程
- **1.15.1** 小组件活跃行支持跳转；Claude 字形按画布占比放大对齐
- **1.15.0** 活跃会话点击跳转所在终端（直传 PID + Codex 桌面端识别）；品牌字形改用官方菜单栏模板图（CSS mask 上色）
- **1.14.0** 图标语言统一：彩色 App 图标块替换为单色品牌字形
- **1.13.0** 小组件拖边缩放 + 选区残留修复
- **1.12.0** 桌面小组件（桌面层玻璃小窗、拖动/位置记忆/点击开面板）
- **1.11.0** 暗化强度设置、Codex 活跃状态推断、会话行操作悬浮重构
- **1.10.0** Ghostty 按 cwd 精确聚焦（借鉴 MioIsland 思路）、面板伸缩全页面适配
- **1.9.0** 点击灵动岛跳转会话所在终端（tty 精确匹配 → cwd → 宿主 App 兜底）
- **1.8.0** 24h 曲线改 Token 用量、双层圆角残角根治
- **1.7.0** 灵动岛停留时长、活跃会话完整路径、圆角阴影修复
- **1.6.0** 设置数值项自定义输入 + 前后端范围钳制
- **1.5.0** 会话完成灵动岛提醒（Claude Stop hook / Codex notify 事件源）
- **1.4.0** 自定义 HUD 玻璃面板替代 NSPopover，面板可伸缩
- **1.3.0** Liquid Glass 化：系统玻璃透出 + 官方规范校准
- **1.2.0** 双 Tab 布局、设置中心、活跃会话监控、额度曲线与用量图重绘、告警通知、菜单栏百分比
- **1.1.0** 打包为标准 App：自包含 bundle / App Support 数据 / SMAppService / DMG
- **1.0.0** 面板初版：额度环 / 会话恢复 / 用量统计 / LaunchAgent 自启
